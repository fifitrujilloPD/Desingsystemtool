# Insumos — Feature 03 Foundations Core-UTP

Colocar aqui los archivos oficiales entregados por diseno/producto para implementar foundations.

## Fuentes esperadas

- Tipografia (JSON o equivalente).
- Colores primary/secondary (JSON).
- Colores semanticos light/dark (JSON).
- **Borders** (JSON o ZIP alineado a Feature 02 `borders.tokens.json` / paquete Borders).
- Iconografia (ZIP/API/repositorio): material, file, alert, flags.
- Logos institucionales UTP (principal, secundario, isotipo, monocromatico; claro/oscuro).

## Reglas

- Versionar los insumos dentro del repo.
- No depender de rutas absolutas locales.
- Registrar version/fecha en cada informe de `../Ejecuciones/`.
